Reconnect reliability fix.

- Reconnects no longer get stuck behind a DNS lookup that was started while the device was offline. Server re-resolution now times out quickly and falls back to the last known address, so toggling Wi-Fi recovers as soon as the network is back.
