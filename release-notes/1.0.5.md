Reliability and routing fixes.

- An empty app allowlist now tunnels nothing instead of routing every app.
- Stopping or reconnecting the tunnel no longer risks a crash.
- The VPN starts reliably even when the app is recreated while the system permission prompt is open.
- Latency checks use the correct port for port-hopping servers and choose the network more reliably.
